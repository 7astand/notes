tableview

board.getAllLayerStacks();
layerstack.getALlLayers();

ComboboxColumnTwo
ComboboxColumnThree

class TableView {
    LayerStack& getSubclass(row);

}

new ComboboxColumnThree(row) {
    LayerStack& stack = tableview.getSubClass(row);
    foreach(Layer* layer, stack.getAllLayers()) {
        addItem(layer->getName());
    }
}

tableView.setText(row, column, "");